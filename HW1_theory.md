# HW1. Архитектурная ката «You Look Good In Print»

## Полученное условие

Сеть локальных копи-центров хочет облачное окружение «всё-в-одном»: создание, редактирование, хранение и печать документов.

- **Requirements**: браузерный или поставляемый клиент, обработка текстов, презентации, шаблоны, версионирование, планирование печати, автоматический и ручной (наличный) расчёт.
- **Users**: тысячи в одном городе на старте, потенциально миллионы.
- **Доп. вводные**: РФ, 2026, сертификация ФСТЭК. Зарубежные облака, платёжные системы и офисный SaaS закрыты, работа через отечественные аналоги.

---

## 1. Отсев требований

Вопросы стейкхолдерам и принятые допущения:

| Вопрос | Допущение |
|---|---|
| Что значит "all-in-one"? Таблицы, OCR, e-sign? | MVP: только текст, презентации, шаблоны. Остальное в фазу 2. |
| Browser vs delivered клиент? | Браузер для конечных клиентов, тонкий клиент в копи-центрах для печатников. |
| Manual payment это что? | Наличные или карта на стойке через ККТ с ОФД (ФЗ-54). |
| Print scheduling: очередь или слоты? | И то, и то: выбор точки + слот выдачи. |
| Кто обработчик ПДн, какой УЗ? | Оператор сервиса. УЗ-2, поскольку есть платёжные данные и потенциальные ПДн третьих лиц в документах. |
| Что разрешено в РФ-2026? | Только отечественные облака, СУБД, IdP, HSM, ОФД, антивирус. Платежи через СБП, ЮKassa, СберPay. |

**Включено в MVP**: веб-редактор текста и презентаций, шаблоны, версии, хранение, заказ печати, оплата онлайн + на стойке, 2FA для оператора.

**Исключено или фаза 2**: таблицы, PDF-редактор, OCR, e-sign, real-time co-editing, нативные мобильные приложения, AI-функции, B2B-кабинет.

---

## 2. Scope Refinement

Пользователи:

1. Конечные клиенты (тысячи → миллионы): создают и редактируют документы, заказывают печать.
2. Печатники в точке (сотни): обслуживают очередь печати своей точки.
3. Кассиры (сотни): принимают наличный/POS-расчёт.
4. Администраторы сети (десятки): точки, тарифы, шаблоны, аналитика.
5. DevSecOps (единицы): эксплуатация.

Функции по ролям:

| Роль | Функции |
|---|---|
| Клиент | Регистрация, 2FA опц., создание документа с нуля или из шаблона, WYSIWYG-редактирование, автосейв, история версий, загрузка собственного файла, заказ печати (точка, формат, копии, слот), оплата (СБП/карта/на стойке), уведомления |
| Печатник | Очередь задач своей точки, печать, изменение статуса |
| Кассир | Поиск задачи по номеру/QR, фискализация оплаты, чек |
| Администратор | CRUD точек/принтеров/тарифов, модерация шаблонов, аналитика выручки и очередей |

Дополнительные функции: программа лояльности, реферальная программа, дашборды BI (DataLens).

Архитектурные модули (черновик):

| Модуль | Функционал |
|---|---|
| Identity / AuthZ | Регистрация, логин, 2FA, RBAC, JWT |
| Document Service | CRUD документов и метаданных |
| Editor Service | Серверная часть редактора (OnlyOffice DocServer / Р7-Офис) |
| Storage Service | Тела документов, версий, превью в S3 |
| Template Library | Каталог шаблонов |
| Print Order Service | Заказ печати, статусы, маршрутизация в точку |
| Shop / Printer Service | Каталог точек, принтеров, расписаний |
| Payment Service | Онлайн-оплата, фискализация |
| Notification Service | Email, SMS, push |
| Admin / BI | Кабинет администратора, аналитика |
| Audit & SIEM | Аудит для ФСТЭК (WORM + SIEM) |

---

## 3. Функциональные требования

### Акторы

Клиент, печатник, кассир, администратор сети, внешние системы (платёжные шлюзы, ОФД, SMS-провайдер, провайдер карт).

### Домен

Пересечение трёх областей: SaaS-офис (редактор + хранилище), маркетплейс печати (выбор точки и слота), e-commerce + офлайн-расчёт.

### Ключевые сущности

```
User(id, email, phone, password_hash, role, has_2fa, pdn_consent)
Document(id, owner_id, title, type, current_version_id, created_at, updated_at, tags)
DocumentVersion(id, document_id, blob_uri, hash, size, created_by, created_at)
Template(id, name, type, blob_uri, preview_uri, category, is_public)
Shop(id, name, address, geo, working_hours, printers[])
Printer(id, shop_id, model, capabilities, state)
PrintOrder(id, user_id, document_version_id, shop_id, copies, options,
           price_kop, pickup_slot, status, created_at)
Payment(id, order_id, method, amount_kop, status, ofd_receipt_id, created_at)
```

### Бизнес-процессы

1. **Создание и печать**: клиент → редактор → автосейв в версии → PrintOrder с точкой и слотом → оплата (онлайн или на стойке) → задача в очередь точки → печатник печатает → READY → клиент уведомлён → HANDED.
2. **Отмена**: до статуса PRINTING с автовозвратом средств.
3. **Жалоба/возврат**: оформление кассиром, корректировочный чек в ОФД.
4. **Срочная печать**: отдельный слот с надбавкой.

### MVP-функции

Клиент: регистрация, редактор, шаблоны, версии, заказ, оплата, пуш. Печатник: очередь, печать, статусы. Кассир: фискализация. Администратор: CRUD точек/принтеров/тарифов, BI-витрина.

### Корнер-кейсы

- Параллельное редактирование в двух вкладках: last-write-wins + предупреждение, версии остаются.
- Падение редактора при автосейве: client-side restore из IndexedDB.
- Удаление документа за минуту до печати: PrintOrder использует snapshot из DocumentVersion, печать выполняется.
- Двойная оплата: идемпотентность по `payment_intent_id`.
- Принтер сломался в момент печати: FAILED, попытка переназначения или возврат денег.
- Файл с вирусом или запрещённым контентом: блокировка Storage Service, событие в SIEM.
- Дубликат фискального чека: retry по идемпотентному ключу + ручная сверка раз в сутки.

### User Stories (фрагмент)

| Роль | Хочу | AC |
|---|---|---|
| Клиент | Видеть карту с ближайшими копи-центрами и временем готовности | Карта подгружается за 2 с, показывает свободные слоты на 24 часа |
| Клиент | Автосохранение документа | Автосейв ≤ 15 с после паузы, индикатор «сохранено» |
| Печатник | Приоритизированную очередь | Сортировка по слоту, срочные сверху, индикация просрочки |
| Администратор | Дашборд выручки и среднего времени по точке | Обновление ≤ 5 мин |
| Офицер ИБ | Аудит действий админов | WORM, 3 года хранения, экспорт в CEF |

---

## 4. Нефункциональные требования

### Производительность

Профиль года 1 (~100k DAU):

- До 10 000 одновременных сессий редактора.
- До 30 000 заказов печати в сутки.
- p95 API CRUD < 200 мс, p95 autosave < 500 мс, локальный ввод символа < 50 мс.

Пик (9-11, 17-19): до 30-50k сессий редактора, до 5 TPS на платёжный сервис.

Сезонные спайки 1 сентября / 1 июня: рост ×3.

### Масштабируемость

- Stateless-сервисы: горизонтально через Kubernetes (Deckhouse или Yandex Managed K8s).
- Document Service: шардирование по `user_id` (Citus, Tantor XL).
- Storage: S3-совместимое, by-design distributed.
- Editor: пул DocServer-инстансов со sticky session по `document_id`.

### Отказоустойчивость

- Multi-AZ внутри российского облака, async-репликация в резервный регион.
- RTO < 30 мин, RPO < 5 мин. Для платежей RPO 0 (sync commit).
- Идемпотентность на оплате и постановке в печать.
- Editor: при недоступности клиент работает в read-only, правки буферизуются в IndexedDB.

### Безопасность (ключевой блок из-за сертификации)

| Слой | Требования |
|---|---|
| Сертификация | ФСТЭК, УЗ-2 для ПДн |
| Транспорт | TLS 1.3, ГОСТ TLS (Кузнечик/Магма) для админ-канала и аудита |
| Покой | ГОСТ-шифрование S3 (КриптоПро NGate) |
| Ключи | HSM ФСБ-сертификации (КриптоПро HSM, ViPNet) |
| AuthN/AuthZ | Клиенты: email+пароль, опц. 2FA. Сотрудники: SSO через корпоративный IdP, обяз. 2FA (TOTP + USB-токен JaCarta/RuToken). RBAC + ABAC |
| Аудит | WORM-bucket для действий администраторов, печатников, кассиров, доступов к ПДн. SIEM (MaxPatrol) |
| Периметр | WAF (PT AF), Anti-DDoS (Servicepipe), антивирус загружаемых файлов (Kaspersky Scan Engine) |
| Уязвимости | SAST/DAST в CI, регулярный пентест, bug bounty |
| Сегментация | Контур ПДн отделён сетевыми политиками, доступ только через bastion с записью сессий |

### Наблюдаемость

OpenTelemetry → Prometheus / VictoriaMetrics (метрики), OpenSearch (логи), Tempo / Jaeger (трейсы), Grafana (UI). Алерты в Telegram-бот + SMS на критичные.

### Регуляторные

- ФЗ-152: ПДн в РФ, согласие при регистрации.
- ФЗ-54: фискализация через ОФД (АТОЛ, Платформа ОФД).
- ФЗ-149: реестр отечественного ПО для тендеров.
- ФЗ-187 (КИИ): при превышении порога значимости категорирование объекта КИИ и подключение к ГосСОПКА.
- ОРИ: при определённой ёмкости хранение метаданных и контента, выдача по запросу.

### Совместимость

- Браузеры: Яндекс.Браузер, Chrome, Safari, Edge. Firefox best-effort.
- Mobile: PWA, адаптивная вёрстка. Редактор приоритетно под desktop.
- API: REST + OpenAPI 3.0, gRPC внутри, WebSocket для редактора.

### UX

- FCP < 1.5 с, редактор открывается ≤ 3 с.
- Автосейв ≤ 15 с с момента паузы.
- WCAG 2.1 AA, screen reader для каталога и заказа.
- Локализация: ru на старте, en в фазу 2.

---

## 5. Ограничения

| Категория | Значение |
|---|---|
| Бизнес-модель | B2C основной поток, B2B-light как фаза 2 |
| DAU / MAU | 100k / 500k на старте, 1M / 5M через 3 года |
| Заказов в месяц | 0.5M → 5M |
| Avg revenue per order | 250 руб. |
| Retention M2 | > 35% |
| География | 1-2 крупных города на старте, миллионники год 2, вся РФ год 3+ |
| Устройства | Desktop-браузер (60%+ редактирования), Mobile PWA (70%+ пользователей), тонкие клиенты в точках (Astra Linux), POS + ККТ у касс |
| Этапы | MVP 6 мес → v1 12 мес (50+ точек, лояльность, аналитика) → v2 24 мес (B2B, мобильное приложение) |
| Развёртывание | Yandex Cloud / VK Cloud / Cloud.ru. Контур ПДн в аттестованном по УЗ-2 сегменте. Резервный регион того же провайдера. |
| Срок до MVP | 9 месяцев + 3 месяца на сертификацию ФСТЭК |
| Бюджет | 60-80 млн руб. до MVP + 20 млн на сертификацию |
| Бюджет на железо | ~12 млн руб./год в облаке при 100k DAU |
| SLA | Веб + редактор 99.9%, заказ + оплата 99.95%, BI 99.5%. Реакция на ИБ-инциденты ≤ 15 мин, 24/7 |
| Стек | Backend: Java 21 (Spring Boot) + Go для Print Order / Payment. Frontend: React 18 + TS, Vite, PWA. Редактор: OnlyOffice DocServer или Р7-Офис SDK. СУБД: Postgres Pro Enterprise. Cache: Redis. MQ: Kafka (Arenadata). Storage: Yandex Object Storage. K8s: Deckhouse. CI/CD: GitLab + ArgoCD + Harbor. Безопасность: PT AF, MaxPatrol SIEM, КриптоПро HSM. |
| Legacy | 1С Розница (интеграция: выручка, остатки бумаги). Принтеры Kyocera/Pantum/Brother через CUPS на тонком клиенте. |
| Интеграции | Платежи: ЮKassa, СБП, СберPay, Тинькофф. ОФД: АТОЛ, Платформа ОФД. SMS: МТС Exolve. Карты: Яндекс.Карты, 2ГИС. Антивирус: Kaspersky Scan Engine. |
| Импортозамещение | Запрещено: AWS, Azure, GCP, Stripe, MS 365, Google Workspace, Datadog, Cloudflare. Используем отечественные аналоги. |

---

## 6. Расчёт нагрузки

### Методология

```
RPS_avg  = DAU × запросов_за_сессию / 86 400
RPS_peak = DAU × запросов_за_сессию × доля_трафика_на_пике / длина_пика_в_сек
```

Исходные данные:

| Параметр | Год 1 | Год 3 |
|---|---|---|
| DAU | 100 000 | 1 000 000 |
| MAU | 500 000 | 5 000 000 |
| Сессий редактора на пользователя в сутки | 1.5 | 1.5 |
| Средняя длительность сессии редактора | 20 мин | 20 мин |
| Заказов печати в сутки | ~30 000 | ~330 000 |
| Запросов CRUD за сессию | 12 | 12 |
| Средний размер документа | 0.8 МБ (текст), 3 МБ (презентация) | то же |
| Размер исходящего PDF на печать | ~2 МБ | ~2 МБ |

### Пользовательский трафик (HTTP CRUD)

| Метрика | Год 1 | Год 3 |
|---|---|---|
| RPS avg | 14 | 140 |
| RPS пик (40% за 4 ч) | 83 | 830 |
| RPS спайк (1 сентября ×4) | 330 | 3 300 |

### Редактор (WebSocket + autosave)

- Одновременных сессий в пик (10% DAU): 10 000 → 100 000.
- ~0.2 op/sec на пользователя.
- С батчингом autosave (раз в 15 с) и debounce: ~700 → ~7 000 RPS.
- Sticky session по `document_id`, in-memory модель документа в инстансе, snapshot в Storage.

### Заказы печати и платежи

| Метрика | Год 1 | Год 3 |
|---|---|---|
| Заказов в сутки | 30 000 | 330 000 |
| TPS средний | 0.35 | 3.8 |
| TPS пик | 0.4 | 4.6 |
| Платежей онлайн (70%) | 21 000/сутки | 230 000/сутки |
| TPS платежей в пик | ~0.3 | ~3 |

### Сетевой трафик

Входящий (загрузки и правки): 100k × 5 версий × 0.8 МБ = **400 ГБ/сутки ≈ 12 ТБ/мес** (год 1), **~120 ТБ/мес** (год 3).

Исходящий (открытие документов + PDF на печать): **~5 ТБ/мес** (год 1), **~50 ТБ/мес** (год 3).

WebSocket-коннекшены в пик: 10 000 (год 1) → 100 000 (год 3, нужен отдельный gateway-слой).

Пиковая полоса: 300-500 Мбит/с → 3-5 Гбит/с.

### Вычислительная мощность (MVP)

| Компонент | vCPU | RAM | Нод | Комментарий |
|---|---|---|---|---|
| API-gateway (NGINX) | 4 | 8 | 3 | 1 запасной |
| Backend CRUD | 4 | 16 | 4 | HPA до 8 |
| Editor Service (DocServer) | 8 | 32 | 3 | Sticky session, RAM-bound |
| Print Order | 2 | 8 | 2 | Низкий QPS, нужна HA |
| Payment | 2 | 8 | 2 | Критичный |
| Notification | 2 | 4 | 2 | Kafka consumer |
| Postgres Pro | 8 | 32 | 3 | Primary + 2 sync |
| Redis | 2 | 8 | 3 | Cache + rate-limit |
| Kafka | 4 | 16 | 3 | Events + outbox |
| ClickHouse | 4 | 16 | 2 | Аналитика |
| OpenSearch | 4 | 32 | 3 | Логи |
| Prometheus + VictoriaMetrics | 4 | 16 | 2 | Метрики |

Итого MVP: **~70 vCPU, ~280 ГБ RAM**. К году 3: **~500-700 vCPU, ~2 ТБ RAM** с автоскейлингом.

### Дисковое пространство

| Тип данных | На старте | Прирост |
|---|---|---|
| Документы | 0.5 ТБ | +6 ТБ/мес (год 1), +60 ТБ/мес (год 3) |
| Версии (live 90 дней, дальше cold) | 2 ТБ | +20 ТБ/мес |
| Шаблоны | 50 ГБ | +10 ГБ/мес |
| PDF-спулы (TTL 7 суток) | 0.5 ТБ | rolling |
| Логи и аудит (3 года хранения) | 200 ГБ/мес | ~7 ТБ итого |
| Метрики | 100 ГБ/мес | downsampling |
| Backup | x1.0 от prod | retention 30 дней |

Итого объектное хранилище: **50-80 ТБ** на старте, **~1 ПБ** к году 3.

### Сводка инфраструктуры

- Compute: ~70 vCPU / 280 ГБ RAM на старте → 500-700 vCPU / 2 ТБ RAM год 3 с автоскейлом.
- Storage: 50-80 ТБ объектного + 2 ТБ блочного → ~1 ПБ объектного к году 3.
- Сеть: 1 Гбит/с (пик ~500 Мбит/с) → 10 Гбит/с.
- WebSocket capacity: 10k → 100k одновременных коннекшенов.
- Резерв: ×1.5 ко всему compute.
